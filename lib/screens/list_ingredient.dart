import 'package:flutter/material.dart';

class IngridientView extends StatelessWidget {
  final DateTime date;
  IngridientView({this.date});
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
